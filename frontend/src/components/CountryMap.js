import React from 'react';

const CountryMap = ({ onCountrySelect }) => {
    return (
        <div>
            <h2>Country Map</h2>
            {/* Map implementation would go here */}
        </div>
    );
};

export default CountryMap;
